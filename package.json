{
  "name": "itslive",
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "workerd", "sharp"]
  },
  "scripts": {
    "dev:api": "pnpm --filter itslive-api dev",
    "dev:serve": "pnpm --filter itslive-serve dev",
    "db:migrate": "pnpm --filter itslive-api exec wrangler d1 migrations apply itslive-prod --remote",
    "deploy:api": "pnpm --filter itslive-api run deploy",
    "deploy:serve": "pnpm --filter itslive-serve run deploy",
    "deploy:queue": "pnpm --filter itslive-queue run deploy",
    "deploy:all": "pnpm db:migrate && pnpm deploy:api && pnpm deploy:serve && pnpm deploy:queue",
    "typecheck": "pnpm --filter itslive-api --filter itslive-serve --filter itslive-queue exec tsc --noEmit"
  }
}
